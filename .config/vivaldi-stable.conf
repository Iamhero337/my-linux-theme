# ============================================
# VIVALDI PERFORMANCE & WAYLAND OPTIMIZATIONS
# ============================================

# --- WAYLAND, DESKTOP INTEGRATION & KEYRING ---
--ozone-platform=wayland
--ozone-platform-hint=wayland
--password-store=gnome-libsecret
--enable-wayland-ime

# --- GPU ACCELERATION (Intel iGPU + NVIDIA dGPU) ---
--enable-gpu-compositing
--enable-gpu-rasterization
--enable-zero-copy
--enable-native-gpu-memory-buffers
--enable-oop-rasterization
--ignore-gpu-blocklist

# --- VIDEO HARDWARE ACCELERATION (VA-API) ---
--enable-accelerated-video-decode
--enable-accelerated-video-encode
--enable-accelerated-mjpeg-decode

# --- STARTUP SPEED ---
--no-first-run
--no-default-browser-check
--disable-sync-preferences
--disable-component-update
--disable-default-apps

# --- NETWORK OPTIMIZATION ---
--enable-quic

# --- FONTS & DISPLAY ---
--force-color-profile=srgb
--enable-font-antialiasing
--enable-subpixel-font-rendering

# --- CONSOLIDATED FEATURES (Stable & Fast Hardware Acceleration) ---
--enable-features=VaapiVideoDecoder,VaapiIgnoreDriverChecks,CanvasOopRasterization,UseSkiaRenderer,WaylandColorManagementV1,BackForwardCache,TabFreezing,TabDiscarding,HighEfficiencyModeAvailable,LazyFrameLoading,ParallelDownloading,TCPFastOpen,DnsOverHttpsUpgrade,FontSrcLocalMatching

# --- CONSOLIDATED DISABLED FEATURES ---
--disable-features=Translate,AutofillServerCommunication,PasswordLeakDetection
